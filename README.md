# Noah Gundotra

Static personal site. GitHub Pages serves the repository root from `master`.

Open `index.html`. There is no build step.

- `index.html`, `styles.css`, `favicon.svg` — the live site
- `assets/` — project stills
- `pages/`, `styles/`, `archive/` — leftover Next.js / Hugo files, not the live site
